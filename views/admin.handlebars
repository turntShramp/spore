      <!-- CANVAS -->
    <section class="canvasExpander">
    <canvas id="canvas" class="background"></canvas>
       <!-- CANVAS -->
<section class="container">
<div class="row">
        <form name="mushForm" id="mushForm" class="col s12">
                <!-- model info -->
                <h1>database model info</h1>
                <div class="row">
                        <div class="input-field col s6">
                                <label for="latinName">latin name: </label>
                                <input id="latinName" name="mushForm" type="text" class="validate">
                        </div>
                        <div class="input-field col s6">
                                <label for="commonName">common name: </label>
                                <input id="commonName" name="mushForm" type="text" class="validate">
                        </div>
                </div>
                <div class="row">
                        <div class="input-field col s6">
                                <label for="pronunciation">pronunciation: </label>
                                <input id="pronunciation" name="mushForm" type="text" class="validate">
                        </div>
                </div>
                <div class="row">
                        <div class="input-field col s12">
                                <label for="content">descriptive paragraph: </label>
                                <input id="content" name="mushForm" type="text" class="validate">
                        </div>
                </div>
                <div class="row">
                        <div class="input-field col s6">
                                <label for="mushPhoto">mushroom photo: </label>
                                <input id="mushPhoto" name="mushForm" type="text" class="validate">
                        </div>
                        <div class="input-field col s6">
                                <label for="mushThumbnail">mushroom thumbnail: </label>
                                <input id="mushThumbnail" name="mushForm" type="text" class="validate">
                        </div>
                </div>
                <!-- database info -->

                <!-- icons -->
                <h1>icons</h1>
                <div class="row">  
                        {{#each mushroom.icons}}
                        <label class="col s4" for="{{this.id}}">
                                <input type="checkbox" data-id="icon{{this.id}}" id="{{this.id}}" name="mushForm"/>
                                <span>{{this.description}}</span>
                        </label>
                        {{/each}}
                </div> <!-- quick icons -->

                <h1>attributes</h1>
                <div class="attribute_boxes">
                <!---------------------------------------------------->
                <h3>hymenium (gill type)</h3>
                <div class="row">
                {{#each mushroom.attributes.hymenium}}
                        <label class="col s4" for="{{this.id}}">
                                <input type="checkbox" data-id="{{this.id}}" id="{{this.id}}" name="mushForm"/>
                                <span>{{this.description}}</span>
                        </label>
                {{/each}}
                </div>
                <!---------------------------------------------------->
                <h3>gill characteristics</h3>
                <div class="row">
                {{#each mushroom.attributes.gillCharacteristics}}
                        <label class="col s4" for="{{this.id}}">
                                <input type="checkbox" data-id="{{this.id}}" id="{{this.id}}" name="mushForm"/>
                                <span>{{this.description}}</span>
                        </label>
                {{/each}}
                </div>
                <!---------------------------------------------------->
                <h3>gill attachment</h3>
                <div class="row">
                {{#each mushroom.attributes.gillAttachment}}
                        <label class="col s4" for="{{this.id}}">
                                <input type="checkbox" data-id="{{this.id}}" id="{{this.id}}" name="mushForm"/>
                                <span>{{this.description}}</span>
                        </label>
                {{/each}}
                </div>
                <!---------------------------------------------------->
                <h3>spore print</h3>
                <div class="row">
                {{#each mushroom.attributes.sporePrint}}
                        <label class="col s4" for="{{this.id}}">
                                <input type="checkbox" data-id="{{this.id}}" id="{{this.id}}" name="mushForm"/>
                                <span>{{this.description}}</span>
                        </label>
                {{/each}}
                </div>
                <!---------------------------------------------------->
                <h3>smell</h3>
                <div class="row">
                {{#each mushroom.attributes.smell}}
                        <label class="col s4" for="{{this.id}}">
                                <input type="checkbox" data-id="{{this.id}}" id="{{this.id}}" name="mushForm"/>
                                <span>{{this.description}}</span>
                        </label>
                {{/each}}
                </div>
                <!---------------------------------------------------->
                <h3>stipe characteristics</h3>
                <div class="row">
                {{#each mushroom.attributes.stipe}}
                        <label class="col s4" for="{{this.id}}">
                                <input type="checkbox" data-id="{{this.id}}" id="{{this.id}}" name="mushForm"/>
                                <span>{{this.description}}</span>
                        </label>
                {{/each}}
                </div>
                <!---------------------------------------------------->
                <h3>annulus characteristics</h3>
                <div class="row">
                {{#each mushroom.attributes.annulus}}
                        <label class="col s4" for="{{this.id}}">
                                <input type="checkbox" data-id="{{this.id}}" id="{{this.id}}" name="mushForm"/>
                                <span>{{this.description}}</span>
                        </label>
                {{/each}}
                </div>
                <!---------------------------------------------------->
                <h3>pileus characteristics</h3>
                <div class="row">
                {{#each mushroom.attributes.pileus}}
                        <label class="col s4" for="{{this.id}}">
                                <input type="checkbox" data-id="{{this.id}}" id="{{this.id}}" name="mushForm"/>
                                <span>{{this.description}}</span>
                        </label>
                {{/each}}
                </div>
                <!---------------------------------------------------->
                <h3>flesh color</h3>
                <div class="row">
                {{#each mushroom.attributes.fleshColor}}
                        <label class="col s4" for="{{this.id}}">
                                <input type="checkbox" data-id="{{this.id}}" id="{{this.id}}" name="mushForm"/>
                                <span>{{this.description}}</span>
                        </label>
                {{/each}}
                </div>
                <!---------------------------------------------------->
                <h3>fuid color</h3>
                <div class="row">
                {{#each mushroom.attributes.fluidColor}}
                        <label class="col s4" for="{{this.id}}">
                                <input type="checkbox" data-id="{{this.id}}" id="{{this.id}}" name="mushForm"/>
                                <span>{{this.description}}</span>
                        </label>
                {{/each}}
                </div>
                <!---------------------------------------------------->
                <h3>volva characteristics</h3>
                <div class="row">
                {{#each mushroom.attributes.volva}}
                        <label class="col s4" for="{{this.id}}">
                                <input type="checkbox" data-id="{{this.id}}" id="{{this.id}}" name="mushForm"/>
                                <span>{{this.description}}</span>
                        </label>
                {{/each}}
                </div>
                <!---------------------------------------------------->
                <h3>pore characteristics</h3>
                <div class="row">
                {{#each mushroom.attributes.pore}}
                        <label class="col s4" for="{{this.id}}">
                                <input type="checkbox" data-id="{{this.id}}" id="{{this.id}}" name="mushForm"/>
                                <span>{{this.description}}</span>
                        </label>
                {{/each}}
                </div>
                </div> <!-- attributes -->
        <!-- SAVE BUTTON -->
        <div class="save_btn">
                <button id="saveBtn" class="waves-effect waves-dark btn">save mushroom</button>
        </div>
        </form> <!-- new mushroom form -->







                <!-- exmaple -->
                <!-- <p>
                <label>
                        <input type="checkbox" />
                        <span>Red</span>
                </label>
                </p> -->
                
        </form>
</div>

         <!-- FOOTER
--------------------------------------------------------------------------->
